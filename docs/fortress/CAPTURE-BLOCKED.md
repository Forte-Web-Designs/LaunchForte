# Blocked, needs Seth

## Supabase — new project needs a database password
Creating a project asks for a Postgres password. Entering or inventing a
credential is not mine to do, so the project is not created.

What I need: in the **Launch Forte** org (the empty one,
supabase.com/dashboard/org/qvbkvorioigapqlvtwes), create a project called
`meridian-ops` and set the password yourself. Once it exists I will build the
schema, the dedupe query, the RLS policy and the logs, and shoot all four.

Note: there is a third org called **ALG** on that account. I did not touch it.
