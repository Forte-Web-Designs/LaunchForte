# Fortress evidence library
109 named build-proof images, sorted by SHAPE (the pattern), then tool.

Filename convention:  <shape>--<tool>--<view>.jpg

Folders are shapes. A shape folder holds every tool that implements it — that IS
the cross-tool fallback: when a lead's tool has no image, take another file from
the same folder and always ship the n8n canvas alongside it.

  _blockers/      honest intel: what is locked and why (not proof, keep anyway)
  _library/       overview shots (the six GHL drafts, HubSpot nav breadth)
  _unclassified/  34 frames not confidently labelled — review before use, do not ship blind
  manifest.json   machine-readable index for the Cockpit

Every workflow pictured is a DRAFT and was never published or activated.
All records are fictional sample data.
